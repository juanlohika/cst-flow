{
  "name": "training-render-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud Run worker that renders training videos from script + audio + slides via ffmpeg",
  "type": "module",
  "scripts": {
    "dev": "tsx src/server.ts",
    "start": "tsx src/server.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "googleapis": "^171.4.0",
    "express": "^4.19.2",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
